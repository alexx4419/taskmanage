{
  "name": "orvix-pro-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend && cd backend && npx prisma generate && npx prisma db push",
    "start": "cd backend && node src/index.js"
  }
}
